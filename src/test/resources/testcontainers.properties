# Force the Docker Desktop named-pipe strategy on Windows so Testcontainers does
# not scan the PATH for docker-machine (a malformed PATH entry can otherwise throw
# java.nio.file.InvalidPathException during strategy detection).
docker.client.strategy=org.testcontainers.dockerclient.NpipeSocketClientProviderStrategy
