# Rate-Limiter
Production-ready distributed rate limiter microservice in Java 17 + Spring Boot 3, backed by Redis with atomic Lua scripts. Supports Fixed Window, Sliding Window Log, and Token Bucket algorithms with per-merchant config, fail-open resilience, and Prometheus/Grafana observability.
